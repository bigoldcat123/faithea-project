## 更新
1.multipart 重命名
```rust
#[derive(MultipartData, Debug)]
struct StuInfo {
    #[faithea(rename="otherInfo")]
    pub other_info: A,
    pub name: Vec<String>,
    pub age: i32,
    pub merried: Option<bool>,
    pub profile: Vec<MultiPartFile>,
}
```
2. searchParam 重命名
```rust
#[get("/searchParam")]
pub async fn search_param(#[search_param("Name")] name: &str, #[search_param] age: Option<String>) {
    format!("name is {} and age is {:?}", name, age)
}
```
