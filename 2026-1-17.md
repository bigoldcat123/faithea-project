# improvement

1. `pub type HandlerModifier = Box<dyn Fn(&mut HandlerTire, &str)>;` + Send and Sync
2. add global error handler!🌟🌟🌟🌟🌟⚠️ 
3. http1.1 header parsing error! json can not be parsed correctly ✅
4. multipart add rename founction macro.
5. websocket add `FromRequest` preHandler impl 🔥🔥🔥🔥
6. `WebSocketDataPayLoad` should use pub(crate)
7. when data too long ,the websocket crash  ✅
8. something unknow : this option thing trigger an error. fix it!
```sh 
  /petRequest/1
  [2026-01-11T04:56:06Z ERROR faithea::error] invalid param: value is missing!
  1 None
```
  ```rust
  #[get("/{page}")]
pub async fn list_requsts(page: i32, #[search_param] query_key: Option<String>) {
    println!("{page} {query_key:?}",);

    match PET_REQUEST_SERVICE.list(page, query_key).await {
        Ok(res) =>{
            Json(Res::ok().data(res).json())
        }
        Err(e) => {
            Json(Res::error().message(e.to_string()).json())
        }
    }
}```
